{
  "name": "glow-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ glow-subgraph-testnet",
    "create-local": "graph create --node http://localhost:8020/ glow-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ glow-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 glow-subgraph",
    "test": "graph test",
    "prettier":"npx prettier --write ./src/**/*"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.59.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "prettier": "^3.0.3"
  }
}
